<?xml version="1.0" encoding="utf-8"?>
<!-- HTTPS only; no user-added CAs are trusted for app traffic. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>
